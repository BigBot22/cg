#include <gtest/gtest.h>
#include <cg/duglas_pecher/duglas_pecher.h>

#include "random_utils.h"

TEST(duglas_pecher, simple)
{
    //EXPECT_TRUE(3 + 4 == sum_of_two(3, 4));
}

TEST(duglas_pecher, simple2)
{
    //EXPECT_TRUE(11 - 4 == sum_of_two(11, 4));
}

TEST(duglas_pecher, simple3)
{
    //EXPECT_TRUE(311 + 4 == sum_of_two(311, 4));
}

